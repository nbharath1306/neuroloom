# NeuroLoom Environment Variables

# Groq API Key (FREE - Get yours at: https://console.groq.com)
# Used for AI-powered article summaries with Llama 3.1 8B
# Free tier: 14,400 requests/day (more than enough for most users)
GROQ_API_KEY=your_groq_api_key_here

# HuggingFace API Token (FREE - Get yours at: https://huggingface.co/settings/tokens)
# Used for premium TTS (Text-to-Speech) with Suno Bark neural voices
# Click "New token" -> "Read" access is enough
# Free tier: ~1000 requests/day (plenty for personal use)
# Models: Suno Bark (best quality) + Microsoft SpeechT5 (fallback)
# Without this, the app uses browser's built-in voices (still good!)
HUGGINGFACE_API_TOKEN=your_huggingface_token_here
