/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['techcrunch.com', 'artificialintelligence-news.com', 'mit.edu'],
  },
}

module.exports = nextConfig
